//Find area of Rectangle Formula : A=wl
#include <stdio.h>
int main(){
    float l,w,area;
    printf("enter length of triangle:");
    scanf("%f",&l);
    printf("enter width of triangle:");
    scanf("%f",&w);
    area=w*l;
    printf("area:%f",area);
}
